---
 - name: Creates a compressed archive with zip extension
   hosts: jenkins217
   tasks:
   - name: Compress directory /path/to/foo/ into /path/to/foo.zip
     archive:
       path: ~/test-via-ansible
       format: zip
